class Main {
	function void main () {
		var int grid_size;
		var Game game;
		let grid_size = Keyboard.readString("Choose a grid size [2-5]: ");
		let game = Game.new(grid_size);
		do game.init();
		return;
	}
}